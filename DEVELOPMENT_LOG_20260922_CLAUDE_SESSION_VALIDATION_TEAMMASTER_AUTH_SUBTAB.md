# HD-20 개발일지 — 가상데이터 검증·팀마스터 개편·인증 간소화·서브탭 세분화 (2026-09-22)

## 목적
`hastom79-hue/hd-20` 실서비스 코드에 대해 (1) 오프라인 검증용 가상데이터 파이프라인을
제공하고, (2) 실제 화면오류를 실서비스 브라우저 조건에서 재현해 수정하고, (3) 팀 마스터
구성을 사용자가 지정한 순서·소속으로 변경하고, (4) 로그인 단계를 사용자 결정에 따라
간소화하고, (5) 정보과잉으로 지목된 서브탭을 실측 기반으로 세분화한다. 모든 변경은
실제 GitHub 저장소에 commit·push하고, 코드 구현·검증·문서 기록을 한 세트로 관리한다.

## 작업 원칙
- 가상데이터는 `?validation=1`일 때만 생성되며, 시드 직전 원본 localStorage를 백업하고
  파라미터 없이 재방문하면 자동 복구한다. Supabase 동기화는 검증 모드에서 격리한다.
- 실제 화면 오류는 추정이 아니라 Playwright(Chromium)로 직접 재현한 뒤 원인을 코드에서
  확인하고 고친다. 재현 없이 코드를 수정하지 않는다.
- 팀 마스터 변경 시 기존 저장 데이터(활동/Audit/Action)의 마이그레이션이 필요한지
  먼저 확인한다. 팀 이름 자체가 바뀌지 않으면 데이터는 그대로 둔다.
- 인증 관련 변경은 사용자가 명시적으로 고른 범위(로그인 단계만 삭제, 사내메일 인증은
  유지)를 벗어나지 않는다.
- 서브탭 세분화는 실측(scrollHeight, 자식 요소 높이)을 근거로 하며, 다른 파일이 기존
  서브탭 키 문자열을 참조하는지 전수 검색으로 확인한 뒤에만 키 이름을 바꾼다.

## 2026-09-22 작업 이력

### 1. 검증용 가상데이터 파이프라인 (`web-validation-fixture.js` v6→v10)
- 요구사항: "가상 데이터를 최대한 많이 생성해줘. 내가 웹의 실행과 오류를 검증할 수
  있게" → 이후 "팀마스터에 맞춰서" 갱신, "1초간 보이다가 사라진다" 재현·수정.
- 구현:
  - `?validation=1`(기본 Activity 960 / Audit 320 / Action 640), `&scale=1~5`,
    `&edge=1`(XSS·잘못된 날짜·중복 ID·미등록 팀 등 경계 케이스), `?validation=reset`.
  - 시드 전 원본 백업(`hd20ValidationBackupV1`) 후 자동 복구, 저장 실패(Quota 초과)
    시 원본으로 롤백.
  - 모든 행에 `source:'web-validation-fixture'` 태깅, ID 접두사 `VALID-`.
  - v9: 팀장 마스터와 동일한 팀 순서/그룹(`group` 필드) 반영.
  - v10: `dashboard-kpi-source.js`의 `isNonProdRow` 수정과 짝을 이뤄 검증 모드에서
    실제로 화면에 반영되도록 함(아래 2번 참고).
- 검증: Node harness로 `dashboard-kpi-source.js`를 직접 로드해 KPI 스냅샷 수치를
  전수 대조. 브라우저(Playwright)로 배너·KPI 카드·운영 KPI 6종·Audit 4상태·Action
  4상태가 실측값과 일치함을 확인.

### 2. 실서비스 화면오류 3건 재현·수정 (커밋 `8c5129c`)
- "전체 웹 구동 테스트 및 오류 검증" 요청에 따라 최신 저장소를 재-clone하여 실제
  코드 그대로 로컬 서버 + Chromium으로 5개 영역 전체를 클릭 순회하며 재검증.
- 버그 A — Audit 대기 오분류: `hd20-native-production-guard.js`가
  `seoulDate(a.auditDate||...)`를 호출할 때 `auditDate`가 없으면 `seoulDate(undefined)`가
  **오늘 날짜**를 반환해, 아직 실시하지 않은 Audit이 "오늘부터 6개월 관리중"으로
  집계됨. `auditStart()` 헬퍼를 추가해 값이 없으면 빈 문자열을 반환하도록 수정.
- 버그 B — 검증데이터가 화면에서 숨겨짐: `dashboard-kpi-source.js`의
  `isNonProdRow()`를 15개 화면 스크립트가 공유하는데, 이 필터가 `web-validation-fixture`
  소스 행도 비운영 데이터로 취급해 검증 모드에서도 화면이 0건으로 보임.
  `?validation=1`일 때는 해당 소스를 정상 데이터로 취급하도록 예외 처리(단,
  `supabase-sync.js`는 자체 필터를 그대로 사용하므로 DB 격리는 영향 없음).
- 팀 마스터 개편(요청 원문 순서/그룹 반영)은 4번 항목 참고.

### 3. FOUC(로그인 게이트 깜빡임) 수정 (커밋 `260b0a4`)
- "1초간 가상데이터가 보이다가 다 사라진다" 재현: `<html>`에 있어야 할
  `class="hd20-auth-pending"`이 누락되어, `html.hd20-auth-pending body{visibility:hidden}`
  CSS 규칙이 처음부터 적용되지 않았음. 대시보드가 먼저 그려졌다가 인증 확인이
  끝나는 순간 로그인 게이트가 뒤늦게 덮여 "사라지는" 것처럼 보임.
  가짜 프로덕션 도메인(`--host-resolver-rules`로 non-localhost 시뮬레이션)으로
  재현·검증 후 `<html>`에 클래스를 복원.

### 4. 팀 마스터 재구성 (커밋 `8c5129c`에 포함)
- 요구사항: 사용자가 제시한 1~16번 순서(조립1팀 10개/조립2팀 6개 소속)를
  `app.js`의 정식 팀 마스터로 반영.
- `app.js`에 `TEAM_DEFS`(순서+소속), `groupNames()/groupOf()/teamsOf()` API 추가.
  16개 팀 이름 자체는 변경 없어 저장된 활동/Audit/Action 데이터 마이그레이션 불필요.
  브라우저에 저장된 이전 표시 순서는 마스터 시그니처가 바뀐 최초 1회만 초기화하고,
  이후 사용자가 직접 바꾼 순서는 보존.

### 5. Supabase 비밀번호 로그인 단계 삭제 (커밋 `67826a5`)
- 요구사항: "수퍼베이스 db로그인 삭제해줘" → 범위 확인 후 "로그인(비밀번호) 단계만
  삭제, 사내메일 인증은 유지" 선택.
- `supabase-auth.js`를 재작성: `@hd.com` 형식 메일 인증 통과 즉시
  `window.HD20_AUTH_BYPASS=true` + `'hd20-auth-bypass'` 이벤트로 앱 진입(localhost
  우회와 동일 패턴 재사용). Supabase 클라이언트 생성·비밀번호 폼·세션 만료 처리를
  모두 제거. 로그아웃은 사내메일 세션만 지우고 새로고침.
- DB 동기화(`supabase-sync.js`, `hd20-db-production-health.js`)는 이미
  `window.HD20_AUTH_BYPASS`에서 안전하게 no-op하도록 되어 있음을 확인하고 그대로 둠 —
  Supabase 로그인 세션이 다시는 생성되지 않으므로 자동으로 비활성 상태 유지.
  더 이상 쓰이지 않는 Supabase JS SDK `<script>` 태그도 `index.html`에서 제거.
- 검증: 가짜 프로덕션 도메인에서 `@hd.com` 즉시 진입/비-`@hd.com` 거부/새로고침 시
  세션 유지/로그아웃 시 게이트 재노출을 모두 실측.

### 6. "1초 깜빡임"·"가상데이터 반영 안됨" 추가 문의 대응
- 사용자가 실제 화면에서 계속 "안 보인다"고 보고 → 스크린샷 요청 후 확인 결과
  검증 데이터는 정상 반영되어 있었음(운영 건전성 KPI 6종 수치가 계산값과 정확히
  일치). 로그인 게이트가 대시보드 위를 덮는 것과 데이터 미반영을 혼동한 것으로 판단,
  코드 변경 없이 원인 설명으로 종결.

### 7. 서브탭 정보과잉 세분화 (커밋 `f253916`)
- 요구사항: "단일 탭 기준 정보가 너무 과잉이다. 탭을 보다 세분화 해줘" →
  "일단 너의 기준대로 세분화해봐".
- 실측(모바일 430px, `?validation=1`, scrollHeight): ⑤ 개선실행/개선조치 80,544px,
  ③ 고도화·표준화 두 서브탭 모두 14,386px 짜리 3조건 분석 위젯 중복, ④ 진단·유지
  Audit 관리 내 3개 독립 작업 혼재.
- 근본 원인은 콘텐츠 부족이 아니라 두 종류의 결함이었음: (a) 이미 만들어진 서브탭
  분리 로직(`*-subtab-dedupe-guard.js`)이 일부 위젯을 어느 서브탭에도 배정하지
  않아 항상 노출되고 있었음, (b) 640행짜리 표 2개가 각각 다른 이유로 페이지네이션이
  누락되어 있었음(CSS 선택자 누락, 클래스 누락). 자세한 내용은 코딩일지 참고.
- ③④⑤ 각각 2탭 → 3탭으로 재구성:
  - ③ 고도화·표준화: 후보 목록 / **3조건 분석(신규)** / 확정·수평전개
  - ④ 진단·유지: 대상 추출 / **실시·점검 입력(신규)** / 유지관리
  - ⑤ 개선실행: 조치 목록 / **팀장 기준정보(신규)** / 효과·재발관리
- 결과: 개선조치 80,544→8,768px, 유지관리 21,138→8,025px. 15개 탭(5영역×2~3서브탭)
  전체 클릭 순회 + edge/scale 스트레스 모드 포함 콘솔 오류 0건.

### 8. 서브탭 추가 세분화 — advancement 3조건 분석 (커밋 `4aee88b`)
- 요구사항: "조금 더 세분화 해도 된다".
- 실측 결과 5영역×2~4서브탭(총 15개) 중 어제 신설한 `advancement.analysis`
  (3조건 분석)가 11,141px로 최대치. 내부를 뜯어보니 요약 위젯 5개는 합쳐서
  1,388px에 불과했고, 라인·작업장별 조건충족 상세표(`.mcaTableWrap`) 하나가
  7,528px를 차지. 그 표 역시 어제 고친 두 표(`.amCases`, `.hd20Lt`)와 동일한
  원인 — `awTable`/`amCases` 클래스가 없어 `table-enhance-suite.js`의
  페이지네이션 스캔 대상에서 제외되어 있었음.
- `advancement`를 3탭 → 4탭으로 재확장: 후보 목록 / 3조건 분석(요약만) /
  **라인·작업장 상세(신규)** / 확정·수평전개. 상세표에 `awTable` 클래스 추가.
- 결과: 3조건 분석 11,141→3,523px, 라인·작업장 상세(신규) 3,246px. 16개 탭
  전체 재검증(edge/scale 포함) 콘솔 오류 0건.

### 9. 서브탭 추가 세분화 2차 — action 조치 목록 / audit 유지관리 (커밋 `57e264c`)
- 요구사항: "더 나눠줘"(⑤ 개선실행/조치 목록이 8,768px로 최대치였음).
- 조치 목록 내부는 개선요청 그리드(amGrid, 3,342px)와 Lead Time 비교
  (hd20LtWrap, 1,026px)라는 서로 다른 관심사가 섞여 있었음. 유지관리 내부는
  진행중 관리(audit6m+awAuditLane)와 종료평가(hd20AuditCloseEvaluation)라는
  성격이 다른 두 워크플로가 섞여 있었음.
- 'retention'(audit), 'manage'(action) 키는 여러 외부 파일이 참조하므로
  이름을 바꾸지 않고, **새 콘텐츠만 새 키로 분리**하는 방식을 유지:
  - audit: 신규 'ongoing'(6개월 관리중) 분리, 'retention'은 종료평가로 범위를
    좁힘 — 외부 evidence-drill 가드들의 원래 의도(종료평가/재발 관련)와
    오히려 더 정확히 일치하게 됨.
  - action: 신규 'leadtime'(처리기간 분석) 분리, 'manage'는 개선요청 그리드로
    범위 유지.
- 결과: audit 3탭→4탭, action 3탭→4탭. 전체 서브탭 16개(어제 15개 대비
  advancement 1개 증가분 포함하면 총 증가), 최대치 8,768→7,728px.
- 구현 중 실수 1건 발견·즉시 수정: `action.leadtime`의 "기한 내 완료" 지표가
  `overdue()` 헬퍼(완료 건은 항상 제외하는 정의)를 잘못 재사용해 "완료"와
  같은 값(399건)이 나왔음. `doneDate<=due` 직접 비교로 수정해 217건으로
  정정, 같은 커밋에 포함해 push.

### 10. "계속 실행 검증" 중 발견한 회귀 — KPI 근거 그리드 전면 회귀 (커밋 `c7b499e`)
- 사용자가 "계속 실행 검증"을 반복 요청 → 이전까지는 탭 전환/scrollHeight만
  검증했고, 각 탭의 "상세 데이터 그리드" 버튼 자체는 클릭 검증하지 않았음을
  인지하고 처음으로 전수 클릭.
- 발견: 어제(대상추출/실시점검입력)와 오늘(3조건분석/라인작업장상세/
  6개월관리중/처리기간분석) 새로 만든 6개 서브탭 전부 그리드 버튼을 눌러도
  "0개 데이터셋"으로 빈 화면이 떴음.
- 원인: `hd20-canonical-grid-guard.js`가 `HD20_SUBNAV.openGrid`를 패치해
  `hd20-kpi-evidence-drill.js`의 `evidence(area,sub,index)`만 쓰도록
  가로채고 있었는데, 이 함수는 `${area}.${sub}.${index}` 조합 문자열의
  switch-case로 구현되어 있어 새 서브탭 키에 대한 case가 아예 없었음.
  기존에 했던 `sub===` 직접 비교 전수 검색으로는 이 조합 문자열 패턴이
  걸리지 않아 놓쳤던 것 — 검색 방법 자체의 사각지대였음.
- 6개 서브탭 전부에 대해 case를 추가(기존 'audit.retention'/'action.manage'
  등 외부 참조가 있는 케이스는 의미를 바꾸지 않고 그대로 둠). 7개 탭 전부
  "4개 데이터셋"으로 정상화, 콘솔 오류 0건 확인.
- 함께 확인: 사내메일 인증 흐름(프로덕션 도메인 시뮬레이션)이 오늘 변경
  이후에도 정상 작동함을 재확인. "연계 흐름" 버튼은 실제 클릭 시 `nav-scroll-
  stability.js`의 지연 스크롤 재조정(최대 320ms)과 Playwright의 클릭-안정성
  대기가 충돌해 자동화 테스트에서만 멈추는 현상을 발견했으나, `onclick`을
  직접 호출하면 정상 동작함을 확인해 애플리케이션 자체의 결함이 아님을
  확인함(실사용자 클릭에는 영향 없음).

### 11. 가상데이터 기본값 ON 전환 (커밋 `0e74799`)
- 요구사항: "벨리데이션 접속말고 내 깃허브에 직접 반영해라" → 의도 확인 질문 후
  "파라미터 없이 그냥 사이트에 들어가만 해도 자동으로 가상데이터가 보이게
  (기본값 변경)"로 확정.
- `web-validation-fixture.js`(v10→v11)의 판정 로직을 반전: 기존
  `MODE==='1'`일 때만 시드 → `MODE==='0'||MODE==='off'`일 때만 건너뛰고 그
  외(파라미터 없음 포함)에는 시드. "원본 복구" 클릭 후 리다이렉트 대상을
  `?validation=0`으로 바꿔, 복구 직후 새로고침해도 즉시 재시딩되지 않도록
  수정(수정 전이라면 여기서 다시 켜졌을 것).
- 동일한 판정 규칙 불일치로 인한 회귀 2건을 전수 검색(`searchParams.get
  ('validation')==='1'` 패턴)으로 찾아 함께 수정: `supabase-sync.js`의 DB
  동기화 격리 조건(기능적 영향은 없음 — Supabase 클라이언트 생성 코드 자체가
  이미 제거된 상태라 방어적 수정), `dashboard-kpi-source.js`의
  `validationMode()`(파라미터 없이 접속 시 fixture는 시딩했는데 KPI 계산에서
  다시 숨겨지는 문제 — 09-22 초반에 고쳤던 것과 동일한 종류의 버그가 새
  기본값 규칙에서 재발한 것을 발견 즉시 수정).
- 검증: 파라미터 없이 접속(배너 v11, Activity 960, KPI 154건 정상) /
  `?validation=0`(진짜 빈 상태) / 기존 `?validation=1` 계열(하위 호환) /
  "가상데이터 끄기" 링크 클릭 후 새로고침 유지 / 가짜 프로덕션 도메인에서
  사내메일 로그인 직후 파라미터 없이 데이터 정상 표시 / edge=1 모드 — 모두
  Chromium으로 확인, 콘솔 오류 0건.

### 12. 탭 구분·중복정보 실사용 피드백 대응 (커밋 `e535a26`)
- 요구사항(실사용 피드백): "1) 세부탭 구분 명확히 어렵고, 2) 탭별 중복정보
  있는거 같음".
- fresh clone 후 16개 탭 전체를 스크린샷으로 직접 훑어 두 문제를 실측 확인:
  - 서브탭 버튼의 활성 표시가 옅은 흰 배경이라 대비가 약함(스크린샷으로 확인).
  - `audit.draw`↔`audit.inspect` 4/4, `advancement.analysis`↔`advancement.detail`
    3/4, `action.manage`↔`action.leadtime` 3/4, `action.manage`↔`action.master`
    3/4 — 4개 탭 쌍에서 메트릭 스트립 숫자가 크게 겹침.
- 탭 활성 표시를 앱 공통 강조색(`--hd-primary #0b5b83`)의 진한 배경 + 하단
  포인터로 변경. 4개 탭의 메트릭을 각 탭 고유 내용 기반으로 재설계.
- 재설계 도중 `hd20-kpi-parity-guard.js`가 메트릭 숫자를 근거 데이터 "행
  개수"로 강제 덮어쓴다는 것을 다시 활용해, 평균/최댓값 같은 비-count 지표는
  설계할 수 없음을 실측으로 발견(“평균 처리일수”가 완료 건수와 같은 값으로
  나타남) → 모든 신규 지표를 count 기반으로 재설계하고 evidence-drill.js의
  대응 케이스도 동일 필터로 동기화. `action.master`는 evidence 케이스 자체가
  없어 그리드가 항상 비어 있던 것도 함께 발견해 신규 추가.
- 검증: 8개 인접 탭 쌍 전수 확인(3개 이상 겹침 없음), 16개 탭 전체 순회 +
  그리드 버튼(팀장 기준정보 포함 전부 4개 데이터셋) 콘솔 오류 0건.

### 13. 대시보드 고도화 맵 버블 겹침 개선 (커밋 `44a5c3f`)
- 요구사항(실사용 피드백): "고도화맵 너무 복잡하다".
- 대시보드 내부 "3. 고도화 맵" 섹션의 "팀별 고도화 Portfolio Map" 버블차트를
  CDP로 직접 캡처해 확인(Playwright의 일반 `screenshot()`이 이 페이지에서
  15초+ 타임아웃되는 것을 발견해 CDP `Page.captureScreenshot`으로 우회).
  16개 팀 버블이 서로 심하게 겹쳐 숫자와 팀 이름이 가려지는 것을 확인.
- 원인: (1) 충돌 회피가 1회성 소폭 넛지뿐이라 값이 비슷한 팀이 몰릴 때
  해소가 안 됨, (2) 모바일에서 지도 높이가 오히려 500px→430px로 줄어드는
  역설계.
- 충돌 회피를 최대 8회 반복·회전 방식으로 개선, 버블 크기 축소, 모바일 지도
  높이를 620px로 확대. 수정 전엔 버블이 서로를 완전히 가렸으나, 수정 후
  16개 버블 모두 숫자 식별 가능함을 확인(팀 이름 라벨 일부는 여전히 인접).
- 부가 발견: 이 페이지는 항상 DOM 노드 약 3만 개를 유지함(비활성 영역도
  DOM에서 제거되지 않고 display:none으로만 숨겨짐). 앱 전체 렌더링 구조에
  관한 것이라 이번 세션 범위 밖으로 별도 기록만 남김.
- 검증: 다른 15개 탭에 영향 없음(scrollHeight·그리드 데이터셋 수치 수정 전과
  동일), 콘솔 오류 0건.

### 14. 고도화 맵 성능 심층 조사 + 종료평가 간헐적 회귀 발견·완화 (커밋 `b8f75bc`)
- 요구사항: "파고들어 고쳐"(버블 클릭 반응 최대 9초+).
- DOM 크기(약 3만 개)는 다른 탭과 거의 같은데 고도화 맵만 유독 느린 것을
  조사한 결과, 이 앱은 28개 파일이 `document.body`/`documentElement`를
  `subtree:true`로 감시하는 MutationObserver를 갖고 있고, 고도화 맵의
  `render()`가 appendChild를 32회(버블 16 + 팀목록 16) 개별 호출해 이
  28개 관찰자를 매번 전부 깨우는 것을 확인. 원본 파일로도 동일 속도임을
  대조해 오늘 이전부터 있던 특성임을 확인.
- `DocumentFragment`로 모아 appendChild 32회→2회로 축소. 초기 렌더 소요가
  수 초 → 0.35초로 단축. 다만 버블 클릭 "이후" 반응은 28개 관찰자 각각을
  감사해야 하는 더 큰 과제라 이번 세션 범위 밖으로 남김.
- 이 조사 과정에서 별도의 간헐적 회귀를 새로 발견: 여러 탭을 빠르게 연속
  이동하면 `종료평가` 탭이 10~50% 확률로 정상(6,514px) 대신 12,000~15,000px
  로 부풀어 보임. 표 자체는 두 경우 모두 정상 페이지네이션 상태였고, 원인은
  어제 작성한 `audit/action-subtab-dedupe-guard.js`의 이전 탭 콘텐츠 숨김
  처리가 `setTimeout(fn,0)`(매크로태스크)이라 새 콘텐츠 렌더링과의 사이에
  진짜 race condition이 있었던 것. `Promise.resolve().then(fn)`(마이크로
  태스크)로 바꿔 빈도를 낮췄으나(10회 중 1회 수준으로 감소) 완전히
  제거하지는 못함 — 스크립트 초고속 연속 클릭에서만 노출되는 좁은 창이라
  실사용 영향은 낮을 것으로 판단, 잔여 리스크로 기록하고 마무리.
- 작업 도중 로컬 미커밋 변경사항이 쌓인 채로 레포 소유자의 커밋 2건이 추가로
  들어와 병합 충돌이 발생 → `index.html`을 원격 최신본 기준으로 다시 받아
  내 2개 캐시 버전 교체만 재적용하는 방식으로 안전하게 해소.

### 15. 대시보드 지표 최우선 배치 (커밋 `d4ca925`)
- 요구사항: "대시보드탭에서는 지표가 가장 최우선으로 디스플레이되도록
  레이아웃을 개선해줘".
- 실측: ① 대시보드 진입 시 헤드라인 KPI 5개 카드(.cards)보다 "OPERATION
  HEALTH" 상세 블록(1,228px)이 먼저 배치되어 있어 핵심 지표를 보려면 큰
  블록을 먼저 지나야 했음.
- `dashboard-priority-layout.js`의 삽입 지점을 `beforebegin`→`afterend`로
  바꿔 헤드라인 지표(.cards)가 OPERATION HEALTH보다 먼저 오도록 순서 변경.
  PERFORMANCE FLOW 라벨이 항상 .cards 바로 앞에 붙도록 삽입 순서도 조정.
- 조사 중 `#hd20Subnav`/`#hd20PurposePanel`/`#hd20OpsMetrics`가 사라지는
  것처럼 보여 회귀로 의심했으나, **원본 미수정 파일로 동일 테스트 시 동일
  현상**임을 확인 후 무관함으로 결론(이 요소들은 대시보드 영역에서는 애초에
  쓰이지 않음). 잘못된 의심을 스스로 반증한 기록.
- 검증: 데스크톱·모바일 스크린샷으로 순서 확인, 16개 탭 전체 재순회 콘솔
  오류 0건.

### 16. 나머지 15개 탭 우선순위 재배치 조사·부분 적용 (커밋 `80b1fd2`)
- 요구사항: "다른 세부탭들도 정보 우선순위에 따라 레이아웃 재배치해줘".
- 15개 탭 전수 조사(블록별 높이·제목 실측) 결과, 대시보드와 동일한 패턴
  (압축 요약 카드가 거대 상세 그리드 **뒤**에 위치)을 가진 곳은
  **③ 확정·수평전개 1곳**으로 확인. "운영상태"(57/47/103/10건 요약)가
  "고도화 후보/공식판정"(154행 상세 표) 뒤에 있던 것을 앞으로 이동.
- ④ 6개월 관리중의 "awAuditLane" 요약 라인도 유사 패턴으로 의심됐으나,
  코드 추적 결과 레거시/불일치 가능성이 있는 복잡한 삽입 경로(⑤로 잘못
  표기된 레이블 등 단서)라 이번엔 건드리지 않고 별도 조사 과제로 남김.
- 조사 과정에서 레이아웃과 무관한 **실제 렌더링 버그**를 발견: ⑤ 개선실행/
  효과·재발관리 탭의 핵심 콘텐츠(효과·재발 검증현황 패널)가 항상 화면에
  없었음. 상단 미니 KPI 스트립에는 같은 수치가 보였기 때문에 지금까지
  발견되지 않았던 것으로 추정. `ensureActionVerify()`가 삽입 앵커로 찾던
  `.awKpis`/`.awFlow` 클래스가 현재 마크업(`.awHero`/`.awActions`로 이미
  리네임됨)에 존재하지 않아 옵셔널체이닝으로 조용히 삽입이 무산되고
  있었음 — 앵커 후보에 `.awHero`를 추가해 해결. 4회 연속 100% 재현되던
  것을 수정 후 정상 렌더링 확인.
- 검증: 16개 탭 전체 재순회 콘솔 오류 0건, 효과·재발관리만 새로 보이게 된
  콘텐츠만큼 scrollHeight 증가(정상), 나머지는 직전 검증치와 동일.

### 17. 스크롤바로 인한 좌우 쏠림·여백 비대칭 보정 (커밋 `abc91fc`)
- 요구사항: "레이아웃 재배치 검토해서 직접 반영 끝났으면 스크롤바 내리면서
  한쪽으로 쏠림과 치우침 여백 생김 보정해줘".
- 원인: `.app{margin:auto}`가 세로 스크롤바 유무에 따라 달라지는 뷰포트
  가용 폭을 기준으로 가운데 정렬됨 — 클래식(비-오버레이) 스크롤바
  브라우저에서 콘텐츠 길이가 짧은 탭(스크롤 없음)과 긴 탭(스크롤 있음)을
  오갈 때마다 좌우 여백이 스크롤바 두께만큼 비대칭해지는 현상.
- 이 샌드박스 기본 헤드리스 브라우저는 오버레이 스크롤바를 써서 재현이
  안 되어, `--disable-features=OverlayScrollbar`로 클래식 스크롤바를
  강제해 실제로 재현·검증.
- `html{scrollbar-gutter:stable both-edges}` 추가. `stable` 단독 적용
  시에는 흔들림은 없앴으나 15px 고정 비대칭이 남아, `both-edges`로
  전환해 완전 대칭 확보.
- 검증: 데스크톱 짧은/긴 탭 모두 좌우 여백 정확히 일치(diff=0), 모바일도
  동일, 16개 탭 전체 재순회 콘솔 오류 0건.

### 18. 탭별 잔여 중복 전수 재검토 + 활동관리 소개 문구 중복 제거 (커밋 `3f6be0f`)
- 요구사항: "아직도 탭별 중복이 보이고 깔끔하게 분석되지 않고 덜 다듬어
  진거 같아 그리도도 정리가 필요해보이는게 많고".
- 16개 탭 전체를 풀페이지 스크린샷으로 육안 재검토. ② 활동관리 탭에서
  "WORK PURPOSE" 패널과 그 아래 awHero 자체 문단이 같은 취지의 소개문을
  두 번 반복하는 것을 확인.
- 이 패턴이 ③④(심지어 "⑤ 진단·유지"로 잘못 표기된 레거시 문자열까지)
  소스에도 존재했으나, 실제 렌더링 스크린샷 대조 결과 ③④⑤는 이 레거시
  블록이 **다른 최신 빌더에 밀려 렌더링되지 않는 죽은 코드**임을 확인 —
  오직 ②만 실제로 중복이 화면에 보이는 유일한 케이스였음.
- ②의 awHero에서 중복 문단만 제거(제목·액션버튼·세부단계 가이드는 유지).
  같은 탭의 "FIELD EXECUTION" 라벨은 실질적으로 다른 정보를 담고 있어
  중복이 아니라고 판단해 그대로 둠.
- 검증: 중복 문구 완전 제거 확인, 관련 2개 탭만 높이 소폭 감소, 나머지
  14개 탭은 직전 검증치와 동일, 콘솔 오류 0건.

### 19. 전직원 대상 쉬운말 전환 (커밋 `7b70f79`)
- 요구사항: "전직원이 다 쓸 웹으로 화면 설명이 너무 어려운 구성이다".
- 전체 앱에서 반복되는 영어 전문용어 소제목 7개를 실사용자(생산현장 전
  직원) 기준 쉬운 한국어로 교체: WORK PURPOSE→화면 안내(16개 탭 공통),
  FIELD EXECUTION→현장 활동, ADVANCEMENT CONTROL→고도화 관리,
  PERFORMANCE FLOW→핵심 지표, OPERATION HEALTH→운영 상태,
  ADVANCEMENT PORTFOLIO→고도화 현황, Canonical Source→실제 데이터 기준.
- ③ 고도화·표준화 배너의 "성과전환 분석, 고도화 후보, GMES 원천데이터,
  공식판정..." 식 개념 나열형 설명문을 "등록된 고도화 후보가 조건을
  얼마나 충족했는지 확인하고, 공식 확정 여부를 판정합니다"로 행동
  중심의 평이한 문장으로 재작성.
- 보류: "①활동등록→⑤고도화수준" 퍼널의 "②고도화후보"/"③공식확정" 두
  항목이 상단 지표와 숫자가 겹치는 문제. 이 마크업이 `확정·수평전개`
  탭에서도 재사용(필터링 기준)되고 있어 항목 삭제 시 그 탭이 깨질 위험을
  확인 — 서브탭별 정밀 분기가 필요해 별도 과제로 이월.
- 검증: 7개 영어 용어 전부 제거 확인, 16개 탭 전체 재순회 콘솔 오류 0건.

### 20. 활동관리 주제별 재구성(삭제 아닌 재배치) (커밋 `03ec959`)
- 요구사항: "같은 주제끼리 묶고 카드/섹션으로 명확히 구분(내용은 유지)".
  사용자가 명시적으로 "삭제가 아니라 재구성"을 원한다고 확인.
- 실제 DOM을 직접 쿼리해 재확인: `hd20AreaHeader→awHero→axBar(Excel
  Import)→hd20ImportAnalysis→awFlow(4단계 흐름가이드)→awGrid(표)→
  adcCard(분석)` 순서였고, `.axBar`가 항상 `.awHero` 바로 뒤에 꽂히도록
  코딩되어 있어 원래 `.awHero` 바로 다음에 와야 할 `.awFlow`(전체 흐름
  안내)를 Excel Import 두 블록이 갈라놓고 있었음. 삭제할 중복이 아니라
  "묶여야 할 것이 흩어진" 문제로 재정의.
- `activity-excel-preview-import.js`의 삽입 앵커를 `.awHero`→`.awFlow`로
  변경(콘텐츠는 1건도 삭제하지 않음). 결과: 안내(배너+제목+액션+흐름
  가이드) → 데이터 가져오기(Excel Import+분석) → 실적 조회(표) → 분석
  (팀별 현황) 순서로 주제별 그룹 완성.
- 검증: 양식 다운로드·Excel 검증 버튼 정상 작동 확인, scrollHeight
  변화 없음(순서만 조정, 삭제 없음 재확인), 16개 탭 콘솔 오류 0건.

## 현재 상태
- 총 20개 commit 모두 `main`에 push, GitHub Pages 재배포 확인.
- 가상데이터 파이프라인, 팀 마스터, 인증 흐름, 서브탭 구조 모두 실제 Chromium
  검증을 거쳤으며 알려진 콘솔 오류는 없음.

## 잔여 과제
1. `?validation=1&scale=3`(활동 2,905건) 대용량에서 Advancement/Audit 팀별 집계
   위젯 등 일부 화면이 7~30초 소요. 기능 정지는 아니나 성능 개선 여지가 있음.
2. 외부 리소스 2건(뉴스 이미지 링크)은 작업 환경 네트워크 정책상 실사용 브라우저에서
   최종 확인하지 못함.
3. `중형Att팀`/`대형Att.팀`의 마침표 표기 불일치는 사용자 확인 대기 중(의도적 유지).
4. 현재 최대 서브탭은 ⑤ 개선실행/조치 목록 8,768px. 추가 세분화 필요 여부는
   사용자 피드백에 따라 결정.
