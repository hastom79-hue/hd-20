(()=>{'use strict';
const ID='hd20OperationalBridge',DRAW='hd20AuditRandomDrawsV1';
function load(k){try{const v=JSON.parse(localStorage.getItem(k)||'[]');return Array.isArray(v)?v:[]}catch{return[]}}
function nonProd(x){if(window.HD20KPIData?.isNonProdRow)return window.HD20KPIData.isNonProdRow(x);const source=String(x?.source||'').toLowerCase(),id=String(x?.id||x?.drawId||'').toUpperCase();return x?.isDemo===true||x?.isTest===true||source==='demo-seed'||source==='e2e-fixture'||id.startsWith('DEMO-')||id.startsWith('E2E-')}
function snapshot(){const draws=load(DRAW).filter(x=>!nonProd(x)),audited=draws.filter(x=>x.auditDate),maintain=audited.filter(x=>String(x.finalEvaluation||x.auditFinalState||'').trim()==='유지'),weak=audited.filter(x=>String(x.finalEvaluation||x.auditFinalState||'').trim()==='미흡');return{maintain,weak}}
function ensure(){const cards=document.querySelector('.cards');if(!cards)return null;let box=document.getElementById(ID);if(!box){box=document.createElement('section');box.id=ID;cards.insertAdjacentElement('afterend',box)}return box}
function render(){const box=ensure();if(!box)return;const s=snapshot();box.innerHTML=`<div class="dobHead"><div><small>EXECUTION RESULT</small><b>6개월 종료평가 결과</b></div><span>실행현황의 중복 건수는 위 Audit·Action Summary에서 확인하고, 여기서는 최종 판정만 표시합니다.</span></div><div class="dobGrid"><article class="ok"><small>종료평가 · 유지</small><strong>${s.maintain.length}건</strong></article><article class="bad"><small>종료평가 · 미흡</small><strong>${s.weak.length}건</strong></article></div><div class="dobFlow">실행 흐름 · Risk 가중 Audit → 실제 Audit 실시일 기준 6개월 관리 → 개선조치 → 효과검증·재발확인 → 종료평가(유지/미흡)</div>`}
function boot(){render();['hd20-kpi-source-updated','hd20-audit-updated','hd20-audit-draw','hd20-action-updated'].forEach(e=>window.addEventListener(e,()=>setTimeout(render,0)))}
window.HD20OperationalBridge={snapshot,render};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();
})();