/* HD-20 Design 2 canonical preview lock.
 * The old implementation rebuilt KPI/framework/analysis DOM from sample values
 * and physically moved existing cards. That behavior is retired.
 * Canonical dashboard modules now own content and placement.
 */
(()=>{'use strict';document.body?.setAttribute('data-approved-preview','design2');})();
