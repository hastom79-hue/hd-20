(()=>{'use strict';
const DRAW='hd20AuditRandomDrawsV1',ACTION='hd20ActionCasesV2';
function load(k){try{const v=JSON.parse(localStorage.getItem(k)||'[]');return Array.isArray(v)?v:[]}catch{return[]}}
function date(v){const d=new Date(v);return Number.isNaN(d.getTime())?null:d}
function add6(v){const d=date(v);if(!d)return null;const day=d.getDate();d.setDate(1);d.setMonth(d.getMonth()+6);d.setDate(Math.min(day,new Date(d.getFullYear(),d.getMonth()+1,0).getDate()));return d}
function done(a){return /완료|확정|종료|종결|close|done/i.test(String(a?.status||a?.activityStatus||a?.judgeState||a?.auditState||''))}
function effectVerified(a){if(a?.effectVerified===true)return true;const v=String(a?.effectState||a?.effectResult||'').trim();if(!v||/대기|미검증|미흡|부적합|무효|false|^0$|^N$/i.test(v))return false;return /^(유효|적합|효과확인|효과확인완료|검증완료|완료|true|1|Y)$/i.test(v)}
function recurred(a){if(a?.recurrence===true)return true;const v=String(a?.recurrenceState??'').trim();if(!v||/^(미발생|없음|미재발|false|0|N)$/i.test(v))return false;return /^(재발|발생|true|1|Y)$/i.test(v)}
function seoulDate(){return new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Seoul',year:'numeric',month:'2-digit',day:'2-digit'}).format(new Date())}
function overdue(a){const due=String(a?.due||a?.targetDate||a?.deadline||'').trim().slice(0,10);return !!due&&!done(a)&&due<seoulDate()}
function closedLoopRecurrence(a){return done(a)&&effectVerified(a)&&recurred(a)}
function stats(){const now=new Date(),allDraws=load(DRAW),audited=allDraws.filter(x=>x.auditDate),actions=load(ACTION);const pending=allDraws.filter(x=>!x.auditDate),active=audited.filter(x=>add6(x.auditDate)>=now),closed=audited.filter(x=>add6(x.auditDate)<now),open=actions.filter(x=>!done(x)),over=actions.filter(overdue),rec=actions.filter(closedLoopRecurrence);return{pending,active,closed,open,over,rec}}
function render(s=stats()){document.querySelectorAll('.sideStack .card').forEach(card=>{const h=card.querySelector('.cardHead h2');if(!h)return;const title=h.textContent||'';if(/Audit 후 6개월 지속관리/.test(title)){h.innerHTML=`Audit 후 6개월 지속관리 <small>(관리중 ${s.active.length}건)</small>`;const rows=[...card.querySelectorAll('.drow')],data=[['6개월 관리 종료',s.closed.length],['6개월 지속관리 중',s.active.length],['재발 발생',s.rec.length]];rows.slice(0,3).forEach((r,i)=>{const span=r.querySelector('span:last-child');if(span)span.innerHTML=`${data[i][0]}<br><b>${data[i][1]}건</b>`})}if(/Action Summary/.test(title)){const rows=[...card.querySelectorAll('.actionRow')],data=[['Audit 실시 대기',s.pending.length],['개선조치 미완료',s.open.length],['기한경과',s.over.length],['재발',s.rec.length]];rows.slice(0,4).forEach((r,i)=>{const spans=r.querySelectorAll('span');if(spans[1])spans[1].textContent=data[i][0];const b=r.querySelector('b');if(b)b.textContent=data[i][1]+'건'})}});return s}
function boot(){render();['hd20-audit-draw','hd20-audit-updated','hd20-action-updated','hd20-refresh-requested'].forEach(e=>window.addEventListener(e,()=>setTimeout(()=>render(),0)));window.addEventListener('storage',e=>{if(e.key===DRAW||e.key===ACTION)setTimeout(()=>render(),0)});document.addEventListener('click',e=>{if(e.target.closest('[data-key="dashboard"]'))setTimeout(()=>render(),30)},true)}
window.HD20DashboardSideSummary={stats,render,recurred,effectVerified,overdue,closedLoopRecurrence,seoulDate};document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot,{once:true}):boot();
})();