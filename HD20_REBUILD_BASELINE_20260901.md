# HD-20 전면개편 Baseline — 2026-09-01

## 목적
본 브랜치는 기존 운영본의 업무 의미를 유지하면서 HD-20을 구조적으로 안정화하고 UI/UX를 전면 개편하기 위한 작업 브랜치이다.

## 절대 유지 항목
- 5S 활동유형: 정리 / 정돈 / 청소 / 시각화 / 위험구역관리 / 5S 고도화
- 공식 고도화 판정주체: 생산혁신팀 + 5S 모듈
- 고도화 3대 판정기준
  1. 시각화·형적관리
  2. 인간공학적 Green Zone
  3. 정량축소·정위치 변경을 통한 공간 활용
- 현장 등록과 공식 확정의 분리
- 확정 이후 +1M / +3M / +6M Lifecycle
- 문제점·개선조치 Workflow 및 AUTO 연계
- 운영 KPI의 의미와 산식
- 7개 사용자 업무 메뉴의 큰 흐름
  1. 대시보드
  2. 성과전환 분석
  3. 5S 활동관리
  4. 고도화 작업장
  5. Audit 관리
  6. 문제점·개선조치
  7. 기준정보

## 현행 구조 진단
현재 index.html은 app.js 외에 다수의 *-hotfix.js, *-final.js, *-recovery.js, *-polish.js를 후순위로 적층하여 동일 DOM과 상태를 여러 스크립트가 보정하는 형태이다. CSS도 styles.css 뒤에 다수의 보정용 CSS가 순차 적층되어 있다.

이 구조의 주요 위험은 다음과 같다.
- 동일 DOM의 중복 렌더링 및 후순위 덮어쓰기
- 전역 변수와 LocalStorage 소스의 다중화
- 기능 수정 시 다른 화면에 회귀 영향
- 렌더링 타이밍 의존 및 새로고침 시 화면 흔들림
- 구형/신형 업무 의미가 코드에 동시에 남아 기준 충돌

## 전면개편 원칙
1. 기존 운영본 main은 직접 대규모 수정하지 않고 본 브랜치에서 검증한다.
2. 새 기능을 기존 코드 위에 hotfix로 추가하지 않는다.
3. 각 업무 화면은 단일 렌더링 진입점과 단일 데이터 소스를 갖도록 정리한다.
4. 화면별 DOM 소유권을 명확히 하여 다른 모듈이 동일 영역을 재생성하지 않게 한다.
5. 공통 디자인 토큰/컴포넌트와 화면 고유 스타일을 분리한다.
6. 사용자 승인 없이 KPI 의미, 판정기준, 판정주체, 5S 유형을 변경하지 않는다.
7. Prototype 데이터와 실제/공식 데이터의 출처를 UI와 코드에서 구분한다.
8. UI 개편보다 먼저 화면 안정성과 데이터 일관성을 확보한다.

## 작업 단계
### Phase 1 — Stabilize
- script/css 적층 구조를 기능별로 분류
- 실제 사용/중복/퇴역 코드를 식별
- 렌더링 충돌, 이벤트 중복, LocalStorage 다중 소스를 제거
- main 화면의 7개 메뉴 전환을 회귀 검증

### Phase 2 — Rebuild Information Architecture
- 7개 메뉴별 화면 목적과 사용자 행동을 재정의
- Dashboard → 분석 → 활동 → 판정/확정 → Audit → 조치 → 기준정보 흐름을 일관되게 연결
- KPI와 Drill-down 경로를 단일화

### Phase 3 — UI/UX Redesign
- 상단 Header / 기간·공장 Filter / Navigation 재설계
- KPI, Chart, Table, Workflow, Modal 공통 컴포넌트 통일
- 업무 중요도에 따른 정보 위계 재배치
- 1600px PC 기준 최적화 + 노트북/태블릿 대응

### Phase 4 — Functional Completion
- GMES 원천 업로드
- 공식 판정/이력
- 1M/3M/6M Audit
- 문제점·개선조치
- Download / Print / Mail
- 기준정보 연계

### Phase 5 — Regression Verification
- 전체 메뉴 이동
- KPI/Raw Data 숫자 일치
- Modal Drill-down
- CRUD
- Reload 안정성
- 중복 이벤트/중복 레코드 생성 여부

## 완료 판정
전면개편 완료는 '화면이 새로워 보이는 것'이 아니라 아래를 모두 만족해야 한다.
- 새로고침 후 화면이 안정적으로 1회 렌더링
- 동일 데이터가 KPI/차트/표/팝업에서 일치
- 7개 메뉴의 주요 기능이 모두 구동
- 공식 판정과 현장 등록이 혼동되지 않음
- Audit → 개선조치 → 재확인 Lifecycle 연결
- 임의 생성 지표/가상 사업장/업무 외 기능 없음
