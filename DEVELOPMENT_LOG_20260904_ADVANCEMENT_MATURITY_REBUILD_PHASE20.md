# HD-20 Development Log — Advancement Maturity Rebuild Phase 20

Date: 2026-09-04

## 목적
③ `고도화·판정` 화면에서 고도화 조건 충족수, 적용 범위, 현상/분석 결과, 공식판정이 한 덩어리로 보이던 구조를 재정리한다. 업무 데이터/판정 로직을 바꾸지 않고 사용자가 `현재 어느 수준까지 올라왔는지`, `어느 범위까지 확산됐는지`, `공식판정과 무엇이 다른지`를 한 화면에서 명확히 읽도록 하는 것이 목적이다.

## 변경 파일
- `maturity-condition-analysis.js`
- `maturity-condition-analysis.css`
- `.github/workflows/design-layout-smoke.yml`
- `README_CURRENT_STATUS_20260904.md`

## 구현 내용
### 1. 고도화 성과 Level Rail
성과 구간을 다음 3단계로 시각적으로 분리했다.

- LEVEL 1: `1개 조건 충족 영역` — 정확히 1개 조건 충족
- LEVEL 2+: `2개 이상 충족 영역` — 정확히 2개 조건 + 3개 모두 충족 누적
- LEVEL 3: `3개 모두 충족 영역` — 3대 조건 모두 충족

상세 데이터에서는 기존처럼 정확히 1/2/3개 조건 충족을 유지한다. 따라서 누적값과 정확값을 혼동하지 않는다.

### 2. 3대 고도화 조건 명시
다음 3개 조건을 화면 중간에 독립적으로 노출한다.

1. 시각화·형적관리
2. 인간공학적 Green Zone
3. 정량축소·정위치 변경을 통한 공간 활용

### 3. 적용범위 계층
`생산팀 → 라인 → 작업장` 관점으로 범위를 읽도록 구성했다.

라인 실적은 구조화된 라인 필드가 있는 경우에만 집계한다. 작업장명/자유기술에서 라인을 추정하지 않는다. 작업장은 있으나 라인 필드가 없는 사례는 `미분류/라인 매핑 필요`로 표시한다.

### 4. 현상/분석결과
실데이터에서 다음을 자동 도출한다.

- 3개 조건 모두 충족 작업장 수
- 2개 이상 조건 충족 사례가 확인된 명시 라인 수
- 1개 조건만 충족한 작업장 후보군
- 라인 미분류 사례 수
- 조건별 적용 편차

임의 임계치, 임의 라인 추정, AI 생성형 추정 문구는 사용하지 않는다.

### 5. 공식판정 분리
화면에 `조건 충족수 ≠ 공식판정`을 명시했다.

판정기재/확정/보완요청/미확정 집계는 실제 판정 상태 필드가 있는 데이터만 대상으로 한다. 고도화 조건 개수만으로 공식 성과를 확정하지 않는다.

### 6. 상세표
상세표에 생산팀, 라인, 작업장/사례, 3대 조건 충족 여부, 충족수, 공식판정을 함께 표시한다. 라인이 없는 경우 `미분류/라인 매핑 필요`를 표시한다.

## Regression 강화
`design-layout-smoke.yml`에 다음 ③ 고도화 계약을 추가했다.

- maturity hierarchy가 실제로 표시될 것
- `1개 조건 충족 영역 / 2개 이상 충족 영역 / 3개 모두 충족 영역` 모두 존재할 것
- `조건 충족수 ≠ 공식판정` 존재
- 3대 고도화 조건 모두 존재
- `라인 미분류 / 매핑 필요` 존재
- `공식판정은 별도 관리` 존재
- Desktop 1440×1000
- 125% 배율 대응 1152×800
- Tablet 900×900
- Mobile 375×812
- 기존 scroll, overflow, Dashboard KPI, Audit, Action, Modal 회귀검증 유지

## 변경하지 않은 것
- canonical store `hd20GMES5SAutoImproveRawV1`
- 공식 판정 주체/프로세스
- Dashboard 승인 KPI 6종
- Audit Risk 가중 랜덤 로직
- Audit 실시일부터 달력 기준 +6개월 지속관리
- 개선조치 7~14일 Deadline 정책
- 임의 라인 추정 금지 원칙
- 승인되지 않은 임계치/위험계수/기간규칙 추가 없음

## 배포
모든 변경은 `main`에 직접 반영되며 기존 GitHub Pages workflow가 자동 배포한다.
