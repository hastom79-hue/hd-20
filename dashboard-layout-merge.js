(()=>{
/* Retired after Design 2 approval.
 * This legacy module rebuilt framework cards, SEQ layout and criteria popup,
 * and wrapped KPI sections into a separate metrics hub. Those behaviors caused
 * layout drift and old terminology to reappear after page load.
 * Current framework/popup behavior is handled by the approved dedicated modules.
 */
window.HD20LegacyLayoutMergeDisabled=true;
})();